/**
 * save.js — kw-package/site-footer
 *
 * Dynamic block — returns null because PHP render.php handles all output.
 * Menu items come from WordPress native navigation (Appearance > Menus).
 */
export default function save() {
	return null;
}
